export interface User {
    name: string,
    photoUrl: string,
    email: string,
    averageSpeed: number,
    lastSpeed: number,
    averageErrors: number,
    lastErrors: number,
    stuckMode: boolean,
}